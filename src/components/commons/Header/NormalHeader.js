import React, { useRef } from 'react'import { AiOutlineMenu, AiOutlinePlusSquare } from 'react-icons/ai'import { Link } from 'react-router-dom'// eslint-disable-next-line react/prop-typesfunction NormalHeader({ hover, active }) {  // eslint-disable-next-line react/prop-types  const { map, grid } = active  const openDropDown = (e) => {    console.log(e)  }  return (    <ul className={'menu'}>      <li className={`menu_btn ${hover}`}>        <button onClick={openDropDown}>          <AiOutlineMenu alt="menu button" />        </button>      </li>      <li className={`menu_link ${hover} ${map}`}>        <Link to="/">지도</Link>      </li>      <li className={`menu_link ${hover} ${grid}`}>        <Link to="/grid">격자</Link>      </li>      <li className={`menu_link btn__add ${hover} `}>        <Link to="/camera">          <AiOutlinePlusSquare />        </Link>      </li>    </ul>  )}export default NormalHeader