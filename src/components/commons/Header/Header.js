import React, { useState, useEffect } from 'react'import { useLocation } from 'react-router-dom'import './Header.scss'import NormalHeader from './NormalHeader'import CameraHeader from './CameraHeader'// eslint-disable-next-line react/prop-typesfunction Header() {  const location = useLocation()  const [hover, setHover] = useState('')  const [active, setActive] = useState({ map: 'menu_active', grid: '' })  const [cameraHeader, setCameraHeader] = useState(false)  const isTouchScreenDevice = () => {    return 'ontouchstart' in window || navigator.maxTouchPoints  }  useEffect(() => {    setCameraHeader(      location.pathname === '/camera' || location.pathname === '/addPost'    )    setHover(isTouchScreenDevice() ? '' : 'menu_hover')    setActive(      location.pathname === '/'        ? { ...active, map: 'menu_active', grid: '' }        : { ...active, map: '', grid: 'menu_active' }    )  }, [location])  return (    <header>      {!cameraHeader ? (        <NormalHeader hover={hover} active={active} />      ) : (        <CameraHeader          hover={hover}          pageName={location.pathname}          history={history}        />      )}    </header>  )}export default Header