import React from 'react'import { AiOutlineLeft } from 'react-icons/ai'// eslint-disable-next-line react/prop-typesfunction CameraHeader({ hover, pageName, history }) {  return (    <ul className={'menu'}>      {/* eslint-disable-next-line react/prop-types */}      <li onClick={() => history.go(-1)} className={`menu_svg ${hover}`}>        <AiOutlineLeft />      </li>      {/* eslint-disable-next-line react/prop-types */}      <li className={`menu__title`}>{pageName.substr(1)}</li>    </ul>  )}export default CameraHeader